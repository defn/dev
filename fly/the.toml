app = "the"

kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[deploy]
strategy = "immediate"

[env]

[experimental]
allowed_public_ports = [22]
auto_rollback = true
entrypoint = ["/service"]
cmd = []

[[services]]
internal_port = 22
protocol = "tcp"

[[services.ports]]
port = 22

[mounts]
source = "the_mnt"
destination = "/mnt"
