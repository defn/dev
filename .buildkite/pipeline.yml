steps:
  - label: kubernetes
    env:
      BUILDKITE_GIT_MIRRORS_PATH: "/cache/git"
    plugins:
      - kubernetes:
          podSpec:
            containers:
              - image: 169.254.32.1:5000/dfd
                imagePullPolicy: Always
                command: [make]
                args:
                  - ci
                securityContext:
                  runAsNonRoot: true
                  runAsUser: 1000
                  runAsGroup: 1000
