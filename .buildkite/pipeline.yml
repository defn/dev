steps:
  - label: nix build
    command: |-
      bash -c '
      set -e
      ./.buildkite/bin/nix-build.sh "$(echo "$(nproc) / 4" | bc)"
      '
  - wait
  - label: bazel build
    command: |-
      bash -c '
      set -e
      source .bash_profile
      cd m
      b build
      '
