steps:
  - command: "env USER=$(id -un) earthly --no-output --push +build-nix"
  - command: "env USER=$(id -un) docker pull quay.io/defn/dev:latest-nix"
  - command: "env USER=$(id -un) earthly --no-output --push +build-devcontainer"
  - command: "env USER=$(id -un) docker pull quay.io/defn/dev:latest-devcontainer"