steps:
  - command: "cd m && export USER=$(id -un) && echo trusted-users = root ubuntu | sudo tee -a /etc/nix/nix.conf && sudo pkill nix-daemon && nix profile install nixpkgs#cachix && cachix use defn && nix develop --command make build"
