{
	"image": "ghcr.io/defn/dev:latest-devcontainer",
	"mounts": [
		"source=work,target=/workspaces",
		"source=nix,target=/nix",
		"source=nix-cache,target=/cache"
	],
	"workspaceMount": "source=${localWorkspaceFolder}/work,target=/w,type=bind,consistency=cached",
	"workspaceFolder": "/w",
	"runArgs": [
		"--privileged",
		"--device=/dev/net/tun",
		"--env-file",
		"${localWorkspaceFolder}/.dotfiles/.env"
	]
}
