{
  "image": "defn/dev:${localEnv:arch}",
  "remoteUser": "ubuntu",
  "runArgs": [
    "-v",
    "/var/run/docker.sock:/var/run/docker.sock",
    "-v",
    "${localEnv:HOME}/.password-store:/home/ubuntu/.password-store",
    "-v",
    "${localEnv:HOME}/work:/home/ubuntu/work"
  ]
}