{
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "ubuntu",
  "runArgs": [
    "-v",
    "/var/run/docker.sock:/var/run/docker.sock",
    "-v",
    "${localEnv:HOME}/.password-store:/home/ubuntu/.password-store"
  ]
}
