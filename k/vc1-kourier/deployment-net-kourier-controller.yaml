apiVersion: apps/v1
kind: Deployment
metadata:
  name: net-kourier-controller
  namespace: knative-serving
spec:
  template:
    metadata:
      labels:
        kuma.io/sidecar-injection: "true"
      annotations:
        kuma.io/mesh: demo
