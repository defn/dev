apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  labels:
    networking.knative.dev/visibility: cluster-local
  name: hello
spec:
  template:
    spec:
      containerConcurrency: 0
      containers:
      - name: hello
        image: defn/bean:latest
        ports:
        - name: h2c
          containerPort: 50051
  traffic:
  - latestRevision: true
    percent: 100
