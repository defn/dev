name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_COLOR: 1
    steps:
    - uses: actions/checkout@v3
    - name: Put back the git branch into git (Earthly uses it for tagging)
      run: |
        branch=""
        if [ -n "$GITHUB_HEAD_REF" ]; then
          branch="$GITHUB_HEAD_REF"
        else
          branch="${GITHUB_REF##*/}"
        fi
        git checkout -b "$branch" || true
    - name: Login to Packages Container registry
      uses: docker/login-action@v1 
      with:
        registry: ghcr.io
        username: not-used
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Download latest earthly
      run: >-
        sudo /bin/sh -c '
          wget 
            -q https://github.com/earthly/earthly/releases/download/v0.6.23/earthly-linux-amd64 
            -O /usr/local/bin/earthly 
          && chmod +x /usr/local/bin/earthly
        '
    - name: Run build
      run: >-
        earthly --ci --push 
          --cache-from ghcr.io/${GITHUB_REPOSITORY}-cache:main 
          --remote-cache ghcr.io/${GITHUB_REPOSITORY}-cache:${GITHUB_REF_NAME} 
            +build
              --tag=${env.GITHUB_REPOSITORY}
