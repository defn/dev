# env KUMA_MODE=zone KUMA_MULTIZONE_ZONE_NAME=grove-0 KUMA_MULTIZONE_ZONE_GLOBAL_ADDRESS=grpcs://100.103.92.51:5685 kuma-cp run 
# k --context k3d-immanent apply -f mesh.yaml
apiVersion: kuma.io/v1alpha1
kind: Mesh
metadata:
  name: default
spec:
  mtls:
    enabledBackend: ca-1
    backends:
      - name: ca-1
        type: builtin
        mode: STRICT # PERMISSIVE
        dpCert:
          rotation:
            expiration: 1d
        conf:
          caCert:
            RSAbits: 2048
            expiration: 10y
