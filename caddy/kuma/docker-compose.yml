services:
    whoami:
        ports:
            - 4999:80
        image: traefik/whoami
        restart: always
    squid:
        build: .
        ports:
            - "3128:3128"
        restart: always