#!/usr/bin/env bash

#MISE description="Run a s6 supervisor"

cd "${MISE_ORIGINAL_CWD}"
mkdir -p svc
(setsid s6-svscan $(pwd)/svc 2>/dev/null &) || true
ln -nfs ../svc.d/main svc/
s6-svscanctl -a svc
