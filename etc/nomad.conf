# nomad agent -config=etc/nomad.conf -data-dir=/tmp/nomad  -dc=grove -region=spiral -node=grove-0 -bootstrap-expect 1
# nomad job run -address http://100.115.182.58:4646 etc/raw_exec.nomad
# nomad job run -address http://100.115.182.58:4646 etc/docker.nomad

server {
  enabled = true
}

client {
  enabled = true
}

plugin "raw_exec" {
  config {
    enabled = true
  }
}
