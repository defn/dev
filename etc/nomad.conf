# nomad job run -detach -address=http://the.tiger-mamba.ts.net:4646 etc/raw_exec.nomad
# nomad job run -detach -address=http://the.tiger-mamba.ts.net:4646 etc/docker.nomad

bind_addr = "0.0.0.0"

server {
  enabled = true
}

client {
  enabled = true
}

plugin "raw_exec" {
  config {
    enabled = true
  }
}

plugin "docker" {
  config {
    endpoint = "tcp://localhost:2375"
  }
}
