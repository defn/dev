lint_task:
  container:
    image: defn/dev:tower
  script: |
    set -efu
    sudo chown -R ubuntu:ubuntu /tmp/cirrus-ci/working-dir
    git config --global --add safe.directory /tmp/cirrus-ci/working-dir
    git checkout -- .dockerignore
    ./bin/e pre-commit run -a
    ps axuf
