#!/usr/bin/env bash

# sudo kumactl install transparent-proxy --kuma-dp-user ubuntu --exclude-outbound-ports 5678 --exclude-inbound-ports 80,443,8000,8888

function main {
	set -efu

	kuma-tp-off || true
	sudo iptables-restore --noflush ~/etc/iptables.txt
}

main "$@"
