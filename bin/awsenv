#!/usr/bin/env bash

function main {
	set -efu

	local account="$1"
	shift

	export AWS_ACCESS_KEY_ID="$(pass AWS_ACCESS_KEY_ID_${account})"
	export AWS_SECRET_ACCESS_KEY="$(pass AWS_SECRET_ACCESS_KEY_${account})"

	exec "$@"
}

main "$@"
