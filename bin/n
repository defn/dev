#!/usr/bin/env bash

case "${1:-}" in
    fmt)
        shift
        exec nixpkgs-fmt "$@"
        ;;
    *)
        exec nix --extra-experimental-features nix-command --extra-experimental-features flakes "$@"
        ;;
esac
