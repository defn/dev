#!/usr/bin/env bash

function main {
	set -efu

	for e in "$@"; do
		case "${e}" in
		--)
			shift
			break
			;;
		*)
			export "${e}=$(pass "${e}")"
			shift
			;;
		esac
	done

	exec "$@"
}

main "$@"
