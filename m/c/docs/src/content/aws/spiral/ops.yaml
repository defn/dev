# auto-generated: transform.sh
account: ops
aws_config: |-
  # auto-generated: aws.cue aws_config_account
  [profile spiral-ops]
  sso_account_id=601164058091
  sso_role_name=Administrator
  sso_start_url=https://d-926760b322.awsapps.com/start
  sso_region=us-west-2

  # auto-generated: aws.cue aws_config_bootstrap
  [profile defn-org]
  sso_account_id=510430971399
  sso_role_name=Administrator
  sso_start_url=https://defn.awsapps.com/start
  sso_region=us-east-2
email: aws-spiral+ops@defn.us
iam_user_access_to_billing: ALLOW
id: "601164058091"
name: spiral-ops
org: spiral
role_name: OrganizationAccountAccessRole
sso_role: Administrator
mise_config: |-
  # auto-generated: aws.cue mise_config
  [env]
  AWS_PROFILE = "spiral-ops"
  AWS_REGION = "us-west-2"
  AWS_CONFIG_FILE = "/home/ubuntu/m/aws/spiral/ops/.aws/config"
aws_config_account: |-
  # auto-generated: aws.cue aws_config_account
  [profile spiral-ops]
  sso_account_id=601164058091
  sso_role_name=Administrator
  sso_start_url=https://d-926760b322.awsapps.com/start
  sso_region=us-west-2
aws_config_bootstrap: |-
  # auto-generated: aws.cue aws_config_bootstrap
  [profile defn-org]
  sso_account_id=510430971399
  sso_role_name=Administrator
  sso_start_url=https://defn.awsapps.com/start
  sso_region=us-east-2
account_readme: |-
  ## AWS Environment: spiral-ops

  ```bash
  cd aws/spiral/ops
  mise trust
  aws sso login
  alogin
  ```

  auto-generated: aws.cue account_readme
infra_org_readme: |-
  ## Organizational-level Terraform: spiral-ops

  ```bash
  cd infra/org-spiral
  mise trust
  aws sso login
  terraform init
  terraform plan
  ```

  auto-generated: aws.cue infra_org_readme
infra_acc_readme: |-
  ## Account-specific Terraform: spiral-ops

  ```bash
  cd infra/acc-spiral-ops
  mise trust
  aws sso login --profile defn-org
  aws sso login
  terraform init
  terraform plan
  ```

  auto-generated: aws.cue infra_acc_readme
infra_acc_terraform: |-
  # auto-generated: aws.cue infra_acc_terraform
  terraform {
    required_providers {
      aws = {
        version = "5.99.1"
        source  = "aws"
      }
    }
    backend "s3" {
      bucket         = "dfn-defn-terraform-state"
  	use_lockfile   = true
      encrypt        = true
      key            = "stacks/acc-spiral-ops/terraform.tfstate"
      profile        = "defn-org"
      region         = "us-east-1"
    }
  }

  provider "aws" {
    profile = "spiral-ops"
    alias   = "spiral-ops"
    region  = "us-east-1"
  }

  data "aws_caller_identity" "current" {
  	provider = aws.spiral-ops
  }

  locals {
    aws_config = jsonencode({
  	"spiral-ops": {
  		account_id = data.aws_caller_identity.current.account_id
  	}
    })
  }

  variable "config" {}

  module "spiral-ops" {
    account   = 510430971399
    name      = "terraform"
    namespace = "spiral"
    stage     = "ops"
    source    = "./mod/terraform-aws-defn-account"
    providers = {
      aws = aws.spiral-ops
    }

    config = var.config
  }

  output "auditor_arn" {
    value = module.spiral-ops.auditor_arn
  }

  output "aws_config" {
    value = local.aws_config
  }
