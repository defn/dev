use flake

PATH="$(pwd)/bin:$(pwd)/node_modules/.bin:$PATH"
for a in $(cd cmd && ls -d */); do
    a="${a%/}"
    PATH="$(pwd)/bazel-bin/cmd/${a}/${a}_:$PATH"
done
PATH="$(pwd)/bazel-bin/meh/bin:$PATH"
PATH="$(pwd)/bin:$PATH"

#export BAZELISK_BASE_URL=https://github.com/aspect-build/aspect-cli/releases/download
#export USE_BAZEL_VERSION=aspect/5.3.4