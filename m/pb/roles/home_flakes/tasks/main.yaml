- name: Install home nix flakes
  shell: (pkill -f -9 baze[l] || true) && cd && source .bash_profile && git pull && mise trust && rm -f m/MODULE.bazel.lock && make nix home
  args:
    executable: /bin/bash
