---
name: dev
services:
  home:
    image: quay.io/defn/dev:base
    user: ubuntu
    working_dir: /home/ubuntu
    privileged: true
    volumes:
      - tailscale:/var/lib/tailscale
volumes:
  tailscale:
