{
  "version": "0.0.1",
  "scripts": {
    "dev": "hugo server --bind=0.0.0.0 --disableFastRender --noHTTPCache --appendPort=false",
    "build": "hugo --minify",
    "clean": "rm -rf public resources .hugo_build.lock"
  },
  "dependencies": {
    "@hyas/doks-core": "^1.0.3"
  }
}
