# Test hello command with config file
# Note: hello command currently uses CLI args, not config
# This test demonstrates the config file setup for future use

-- defn.yaml --
hello:
  name: ConfigUser

-- test.sh --
# For now, hello uses CLI args not config
# This is a placeholder for when we add config support
exec defn hello Bob
stdout 'name=Bob'
