# Test hello command with config file

-- defn.yaml --
hello:
  name: ConfigUser

# Test with config file (no CLI arg) - should use config value
exec defn hello
stdout 'name=ConfigUser'

# Test with CLI arg - should override config value
exec defn hello Bob
stdout 'name=Bob'
