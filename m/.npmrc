loglevel=error

# Reject packages published less than 7 days ago (security: avoid typosquatting/malicious packages)
minimum-release-age=7d

# pnpm hoisting settings for monorepo compatibility
shamefully-hoist=false
hoist=true
hoist-pattern=['*']
public-hoist-pattern=['*eslint*', '*prettier*']