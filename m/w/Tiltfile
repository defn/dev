#!/usr/bin/env python

load("../common/Tiltfile", "main")

local_resource(
    "build",
    cmd=[
        "bash",
        "-c",
        """
            exec make build
        """,
    ],
)

local_resource(
    "dev",
    serve_cmd=[
        "bash",
        "-c",
        """
            exec make dev
        """,
    ],
)
