#ManagedBy: cue

apiVersion: v1
kind: Service
metadata:
  name: traefik
  namespace: traefik
  annotations:
    external-dns.alpha.kubernetes.io/internal-hostname: '*.district.amanibhavam.defn.run, *.default.district.amanibhavam.defn.run, *.coder.district.amanibhavam.defn.run'
spec:
  type: ClusterIP
