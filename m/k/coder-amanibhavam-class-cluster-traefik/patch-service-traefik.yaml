#ManagedBy: cue

apiVersion: v1
kind: Service
metadata:
  name: traefik
  namespace: traefik
  annotations:
    external-dns.alpha.kubernetes.io/internal-hostname: '*.class.amanibhavam.defn.run, *.default.class.amanibhavam.defn.run, *.coder.class.amanibhavam.defn.run'
spec:
  type: ClusterIP
