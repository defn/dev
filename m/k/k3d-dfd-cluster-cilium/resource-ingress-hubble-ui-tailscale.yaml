#ManagedBy: cue

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hubble-ui-ts
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - bubble
  defaultBackend:
    service:
      name: hubble-ui
      port:
        number: 80
