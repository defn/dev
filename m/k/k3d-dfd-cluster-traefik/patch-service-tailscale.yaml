#ManagedBy: cue

apiVersion: v1
kind: Service
metadata:
  name: traefik
  namespace: traefik
  annotations:
    external-dns.alpha.kubernetes.io/internal-hostname: traefik.dev.amanibhavam.defn.run
spec:
  type: ClusterIP
