#ManagedBy: cue

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: traefik-tailscale
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - traefik
  defaultBackend:
    service:
      name: traefik
      port:
        number: 80
