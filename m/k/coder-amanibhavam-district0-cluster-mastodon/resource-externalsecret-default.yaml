#ManagedBy: cue

apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: mastodon-default
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: coder-amanibhavam-district0
  target:
    name: mastodon-default
    creationPolicy: Owner
  data:
    - secretKey: MASTODON_ADMIN_PASSWORD
      remoteRef:
        key: coder-amanibhavam-district0-cluster
        property: mastodon_admin_password
    - secretKey: OTP_SECRET
      remoteRef:
        key: coder-amanibhavam-district0-cluster
        property: mastodon_otp_secret
    - secretKey: SECRET_KEY_BASE
      remoteRef:
        key: coder-amanibhavam-district0-cluster
        property: mastodon_secret_key_base
