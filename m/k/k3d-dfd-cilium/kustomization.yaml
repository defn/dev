#ManagedBy: cue

patches: []
namespace: kube-system
resources: []
helmCharts:
  - releaseName: cilium
    namespace: kube-system
    valuesInline:
      operator:
        replicas: 1
      hubble:
        relay:
          enabled: false
        ui:
          enabled: false
    name: cilium
    version: 1.14.0
    repo: https://helm.cilium.io
    includeCRDs: true
