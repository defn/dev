#ManagedBy: cue

patches: []
resources: []
commonLabels: {}
commonAnnotations: {}
helmCharts:
  - releaseName: bootstrap
    name: any-resource
    version: 0.1.0
    repo: https://kiwigrid.github.io
    valuesInline:
      anyResources:
        external-secrets: |
          apiVersion: argoproj.io/v1alpha1
          kind: Application
          metadata:
            namespace: argocd
            name: coder-amanibhavam-district0-cluster-external-secrets
            annotations:
              argocd.argoproj.io/sync-wave: "100"
          spec:
            project: default
            destination:
              name: coder-amanibhavam-district0-cluster
            source:
              repoURL: cache.defn.run:5000
              chart: library/helm/coder-amanibhavam-district0-cluster-external-secrets
              targetRevision: 0.0.1
            syncPolicy:
              automated:
                prune: true
                selfHeal: true
              syncOptions: []
        kyverno: |
          apiVersion: argoproj.io/v1alpha1
          kind: Application
          metadata:
            namespace: argocd
            name: coder-amanibhavam-district0-cluster-kyverno
            annotations:
              argocd.argoproj.io/sync-wave: "100"
          spec:
            project: default
            destination:
              name: coder-amanibhavam-district0-cluster
            source:
              repoURL: cache.defn.run:5000
              chart: library/helm/coder-amanibhavam-district0-cluster-kyverno
              targetRevision: 0.0.1
            syncPolicy:
              automated:
                prune: true
                selfHeal: true
              syncOptions:
                - ServerSideApply=true
        mastodon: |
          apiVersion: argoproj.io/v1alpha1
          kind: Application
          metadata:
            namespace: argocd
            name: coder-amanibhavam-district0-cluster-mastodon
            annotations:
              argocd.argoproj.io/sync-wave: "100"
          spec:
            project: default
            destination:
              name: coder-amanibhavam-district0-cluster
            source:
              repoURL: cache.defn.run:5000
              chart: library/helm/coder-amanibhavam-district0-cluster-mastodon
              targetRevision: 0.0.2
            syncPolicy:
              automated:
                prune: true
                selfHeal: true
              syncOptions: []
        postgres-operator: |
          apiVersion: argoproj.io/v1alpha1
          kind: Application
          metadata:
            namespace: argocd
            name: coder-amanibhavam-district0-cluster-postgres-operator
            annotations:
              argocd.argoproj.io/sync-wave: "100"
          spec:
            project: default
            destination:
              name: coder-amanibhavam-district0-cluster
            source:
              repoURL: cache.defn.run:5000
              chart: library/helm/coder-amanibhavam-district0-cluster-postgres-operator
              targetRevision: 0.0.1
            syncPolicy:
              automated:
                prune: true
                selfHeal: true
              syncOptions: []
    includeCRDs: true
