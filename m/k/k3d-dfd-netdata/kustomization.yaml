#ManagedBy: cue

patches: []
namespace: netdata
resources:
  - resource-namespace-netdata.yaml
  - resource-ingress-netdata.yaml
helmCharts:
  - releaseName: netdata
    namespace: netdata
    valuesInline:
      ingress:
        enabled: false
    name: netdata
    version: 3.7.69
    repo: https://netdata.github.io/helmchart
    includeCRDs: true
