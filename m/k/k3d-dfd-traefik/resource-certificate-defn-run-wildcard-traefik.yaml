#ManagedBy: cue

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: defn-run-wildcard
  namespace: traefik
spec:
  secretName: defn-run-wildcard
  dnsNames:
    - '*.dev.amanibhavam.defn.run'
    - '*.default.dev.amanibhavam.defn.run'
  issuerRef:
    name: zerossl-production
    kind: ClusterIssuer
    group: cert-manager.io
