#ManagedBy: cue

patches:
  - path: jsonp-deployment-operator-increase-logging.yaml
    target:
      kind: Deployment
      name: operator
      namespace: tailscale
  - path: jsonp-secret-operator-oauth-remove.yaml
    target:
      version: v1
      kind: Secret
      name: operator-oauth
      namespace: tailscale
resources:
  - https://raw.githubusercontent.com/tailscale/tailscale/main/cmd/k8s-operator/deploy/manifests/operator.yaml
  - https://raw.githubusercontent.com/tailscale/tailscale/main/cmd/k8s-operator/deploy/manifests/authproxy-rbac.yaml
  - resource-externalsecret-tailscale.yaml
  - resource-clusterrolebinding-tailscale-admins.yaml
commonLabels: {}
commonAnnotations: {}
