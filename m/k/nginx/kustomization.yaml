#ManagedBy: cue

patches:
  - path: patch-service-nginx.yaml
namespace: nginx
resources:
  - resource-namespace-nginx.yaml
helmCharts:
  - releaseName: nginx
    namespace: nginx
    valuesInline: {}
    name: nginx
    version: 15.1.1
    repo: https://charts.bitnami.com/bitnami
    includeCRDs: true
