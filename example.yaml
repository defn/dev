apiVersion: apps/v1
kind: Deployment
metadata:
  name: example
spec:
  replicas: 10
  selector:
    matchLabels:
      app: example
  template:
    metadata:
      labels:
        app: example
    spec:
      containers:
        - name: ubuntu
          image: ubuntu
          command: ["tail", "-f", "/dev/null"]
